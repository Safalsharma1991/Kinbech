services:
  - type: web
    name: kinbech
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host=0.0.0.0 --port=10000
    runtime: python
    pythonVersion: 3.10.0  # ✅ This line is important
    plan: free
    region: oregon
    branch: main
